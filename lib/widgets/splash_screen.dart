import 'package:flutter/material.dart';

class SplashScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        // Pode mostrar: logotipo, alguma imagem ...
        child: Text('Loading...'),
      ),
    );
  }
}
